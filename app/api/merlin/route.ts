import { fetchMerlin, type MerlinType } from '@/lib/merlin';
export async function GET(req:Request){try{const p=new URL(req.url).searchParams;const {source,text}=await fetchMerlin({type:p.get('type') as MerlinType,start:p.get('start')||'',end:p.get('end')||''},req.signal);return Response.json({source,text},{headers:{'Cache-Control':'no-store','Access-Control-Allow-Origin':'*'}});}catch(e){return Response.json({error:e instanceof Error?e.message:'MERLIN retrieval failed.'},{status:502,headers:{'Access-Control-Allow-Origin':'*'}});}}
